# Copy to keystore.properties (gitignored) and fill in for local release signing.
# For GitHub Actions, use repository secrets instead (see README).
storeFile=../path/to/release.keystore
storePassword=yourStorePassword
keyAlias=yourKeyAlias
keyPassword=yourKeyPassword
